{
  "version": 2,
  "builds": [
    {
      "src": "./index.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [{ "source": "/server/(.*)", "destination": "/server" }],
  "redirects": [{ "source": "/server/(.*)", "destination": "/server" }]
}
